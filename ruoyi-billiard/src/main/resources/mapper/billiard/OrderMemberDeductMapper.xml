<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.billiard.mapper.OrderMemberDeductMapper">
    
    <resultMap type="OrderMemberDeduct" id="OrderMemberDeductResult">
        <result property="orderMemberDeductId"    column="order_member_deduct_id"    />
        <result property="orderId"    column="order_id"    />
        <result property="memberId"    column="member_id"    />
        <result property="totalAmount"    column="total_amount"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectOrderMemberDeductVo">
        select order_member_deduct_id, order_id, member_id, total_amount, create_by, create_time, update_by, update_time from t_order_member_deduct
    </sql>

    <select id="selectOrderMemberDeductList" parameterType="OrderMemberDeduct" resultMap="OrderMemberDeductResult">
        <include refid="selectOrderMemberDeductVo"/>
        <where>  
            <if test="orderId != null "> and order_id = #{orderId}</if>
            <if test="memberId != null "> and member_id = #{memberId}</if>
            <if test="totalAmount != null "> and total_amount = #{totalAmount}</if>
        </where>
    </select>
    
    <select id="selectOrderMemberDeductByOrderMemberDeductId" parameterType="Long" resultMap="OrderMemberDeductResult">
        <include refid="selectOrderMemberDeductVo"/>
        where order_member_deduct_id = #{orderMemberDeductId}
    </select>

    <insert id="insertOrderMemberDeduct" parameterType="OrderMemberDeduct">
        insert into t_order_member_deduct
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="orderMemberDeductId != null">order_member_deduct_id,</if>
            <if test="orderId != null">order_id,</if>
            <if test="memberId != null">member_id,</if>
            <if test="totalAmount != null">total_amount,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="orderMemberDeductId != null">#{orderMemberDeductId},</if>
            <if test="orderId != null">#{orderId},</if>
            <if test="memberId != null">#{memberId},</if>
            <if test="totalAmount != null">#{totalAmount},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateOrderMemberDeduct" parameterType="OrderMemberDeduct">
        update t_order_member_deduct
        <trim prefix="SET" suffixOverrides=",">
            <if test="orderId != null">order_id = #{orderId},</if>
            <if test="memberId != null">member_id = #{memberId},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where order_member_deduct_id = #{orderMemberDeductId}
    </update>

    <delete id="deleteOrderMemberDeductByOrderMemberDeductId" parameterType="Long">
        delete from t_order_member_deduct where order_member_deduct_id = #{orderMemberDeductId}
    </delete>

    <delete id="deleteOrderMemberDeductByOrderMemberDeductIds" parameterType="String">
        delete from t_order_member_deduct where order_member_deduct_id in 
        <foreach item="orderMemberDeductId" collection="array" open="(" separator="," close=")">
            #{orderMemberDeductId}
        </foreach>
    </delete>
</mapper>