<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.generator.mapper.GenCustomMapper">


    <select id="selectTableList" parameterType="string" resultType="map">
        ${sql}
    </select>

    <select id="selectTableMap" parameterType="string" resultType="map">
        ${sql}
    </select>

    <insert id="insertTable" parameterType="string">
        ${sql}
    </insert>

    <update id="updateGenTable" parameterType="string">
        ${sql}
    </update>

    <update id="deleteTableByIds" parameterType="string">
        ${sql}
    </update>

</mapper>