环境信息：
1、服务部署地址： 192.168.25.97
2、后端部署根路径：/home/wss/zj/ruoyi
3、服务分支分支路径：
/home/wss/zj/ruoyi/weifang 潍坊后端服务
/home/wss/zj/ruoyi/carbon  碳计量后端服务

服务发布流程：
1、修改代码
2、本次测试通过后，提交代码到个人远程分支
3、发起pull request请求，等待审核通过
4、审核通过后，合并代码到主分支（审核人操作）
5、切换到主分支代码，并pull最新代码
6、启动主分支服务，本地测试通过后，发布服务到服务器；

注意：在本地测试和发布服务时，需要切换redis和mysql的连接信息。
